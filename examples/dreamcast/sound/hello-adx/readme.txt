This example expects a file called "test.adx" to exist in the root of the
romdisk. Put a small, compatible compressed-audio file with that name in the
romdisk directory before compiling if you want the example to work. ;)
