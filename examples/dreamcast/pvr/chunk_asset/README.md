# Compact-model fixture

This core-fiber bundle retains `chunk-asset-model.obj` for the direct-disc
asset benchmark in `../chunk_asset_disc`. The Service Executor-based
`chunk-asset` application is available in `addon/fiber-service-sh4zam`, not
this core-only provider bundle. Ordinary LZ4 and compact-asset decoding remain
available; only the executor-specific wrapper and examples are excluded.
